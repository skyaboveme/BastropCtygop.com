---
import BaseLayout from './BaseLayout.astro';
import Header from '../components/layout/Header.astro';
import Footer from '../components/layout/Footer.astro';
import Breadcrumbs from '../components/layout/Breadcrumbs.astro';
import DonationPopup from '../components/ui/DonationPopup.astro';

interface Props {
  title: string;
  description?: string;
  ogImage?: string;
  jsonLd?: object;
  showBreadcrumbs?: boolean;
}

const { title, description, ogImage, jsonLd, showBreadcrumbs = true } = Astro.props;
---

<BaseLayout title={title} description={description} ogImage={ogImage} jsonLd={jsonLd}>
  <Header slot="header" />
  {showBreadcrumbs && <Breadcrumbs />}
  <slot />
  <Footer slot="footer" />
  <DonationPopup />
</BaseLayout>
